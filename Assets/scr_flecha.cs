using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scr_flecha : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //print("aqifesrf :  "+  FindObjectOfType<Canvas>().GetComponent<RectTransform>().rect.height);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
